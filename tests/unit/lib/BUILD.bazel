"""Tests for the util submodule."""

load("//tools:rules_cc.bzl", "lucid_cc_googletest")

lucid_cc_googletest(
    name = "test_eigen",
    tags = [
        "lib",
        "unit",
    ],
    deps = ["//lucid/lib:eigen"],
)
